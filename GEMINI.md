# تعليمات هيكلية ونظام العمل (قانون حماية إلزامي للذكاء الاصطناعي)

أنت الآن تعمل في بيئة اختبار نظام المبيعات الخاص بـ "الشركة". يحتوي هذا المشروع على بنية حساسة جداً وقواعد أعمال (Business Rules) تم التعب فيها طويلاً. **ممنوع منعاً باتاً المساس بالأسس التالية أو تغييرها دون إذن صريح من المستخدم.**

## 🚨 1. تحذير خطير بخصوص `finance-core.js` (فخ النطاق - Scope Trap)
1. **نطاق المتغيرات:** جميع المتغيرات والدوال الأساسية في النظام (مثل `pendingSales`, `accounts`, `logOperation`, `products`) معرّفة **بداخل** مستمع الحدث `document.addEventListener("DOMContentLoaded", function() { ... })`.
2. **يُمنع منعاً باتاً** إضافة أي كود جديد أو دوال جديدة في نهاية الملف (في القاع) لأنها ستكون خارج النطاق الكلي ولن تتعرف على أي متغيرات وستعطي `ReferenceError`.
3. **أين تضع الأكواد؟** أي دالة جديدة أو تعديل يجب أن يُحقن **قبل** إغلاق المستمع والذي يقع في الكود عند التعليق `}); // End DOMContentLoaded`.

## 🛡️ 2. حماية التعديلات الجوهرية (ممنوع التخريب)
لقد قمنا ببناء عدة ميزات معقدة ومترابطة، يجب الحفاظ عليها كما هي:

### أ. التحويل لدين (Convert Pending Sale to Debt)
- **الدالة المسؤولة:** `window.convertPendingSaleToDebt` (مسندة لـ window من داخل DOMContentLoaded).
- **المنطق:** تقوم الدالة بحذف البيعة من `pendingSales`، وتضيف الدين مباشرة إلى حساب العميل في مصفوفة `customers` عبر `push` داخل سجل العميل.
- **سجل العمليات (General Ledger):** يجب أن تقوم بتسجيل العملية فوراً باسم `"تحويل لدين"` عبر `logOperation`.
- **التراجع (Undo):** يجب أن تكون الدالة مدعومة بنظام التراجع عبر استدعاء `saveStateToHistory()` في أول سطر، و `updateUndoRedoButtons()` في آخرها.

### ب. المرتجعات اللوجيستية للبيعات المؤقتة (Pending Returns Logistics)
- **الزر والدالة:** عند الضغط على زر "مرتجع" لبيعة مؤقتة (`initiate-return-from-pending`)، **لا يتم حذف البيعة فوراً**، بل يجب أن يفتح الـ Modal الأحمر (`cancelPendingSaleModal`).
- **التعبئة التلقائية للمودال:** يقوم النظام بقراءة `saleData` واستخراج `mainProduct` و `additionalItems` ليطبعها بداخل `cancel-pending-items-list`.
- **النقل اللوجيستي (Pending Receipt):** عند تأكيد الاسترداد في المودال، تنتقل العملية إلى مصفوفة `pendingReturns`. هذه عملية **نقل لوجيستي** وليست خسارة مالية.
- **الحسابات (Accounting):** التكلفة الأصلية (`originalCost`) للبضاعة محفوظة. يتم فقط خصم ربح البيعة الوهمي من الحسابات العامة، في حين تبقى التكلفة كـ "أصل" قيد الاستلام. يتم حساب الأصول المعلقة `pendingReturnsAssetValue` بناءً على `costPrice` وليس `salePrice`.

### ج. واجهة المرتجعات (Returns UI)
- **تمييز البيعات المؤقتة:** دالة `updatePendingReturnsDisplay` مصممة بذكاء لاكتشاف المرتجعات اللوجيستية عبر شرط `if (!!ret.originalSaleData)`.
- **طريقة العرض:** تقوم بعرض الأصناف بشكل قائمة نظيفة (1x صنف بتكلفته)، مع تظليل برتقالي وعلامة **"⚠️ بيعة مؤقتة مرتجعة"**.
- **منع التلاعب:** تحتوي الواجهة على عبارة *"الأرباح ملغاة، التكلفة محفوظة كأصل"* و *"نقل لوجيستي فقط"* لتمييزها عن المرتجعات العادية. **لا تقم بتعديل هذا التصميم.**

### د. الحماية من النقر المزدوج (Double-Click Bug Protection)
- **منطق الدوال الحساسة:** في دوال مثل `handleConfirmReceipt`، يجب **دائماً** التحقق من وجود العنصر أولاً عبر `findIndex` قبل اتخاذ أي إجراء.
- **تجنب تكرار اللقطات:** إذا لم يُعثر على العنصر (مما يعني أنه تم الضغط على الزر مرتين بسرعة)، يجب عمل `return` صامت وعدم استدعاء `saveStateToHistory()` وعدم إظهار أي رسائل خطأ للمستخدم.

### هـ. هيكلة الملفات (Directory Structure)
- **تنظيم الملفات:** المشروع يعتمد الآن على هيكلة منظمة. ملفات `js` موجودة في مجلد `js/` وملفات `css` في مجلد `css/`.
- **عدم العشوائية:** أي تعديلات على الروابط في الواجهة `finance.html` يجب أن تراعي المسارات الصحيحة (`js/finance-core.js` كمثال). **لا تنشئ** أي ملفات برمجية في المجلد الرئيسي أبداً.

### و. سجل وتاريخ المبيعات المؤقتة (Pending Sales Edit Log)
- **منع تصفير التاريخ:** عند التعديل على أي بيعة مؤقتة، **يُمنع منعاً باتاً** طمس أو تعديل التاريخ الأصلي (`timestamp`) للبيعة. يجب استخدام `originalSale.timestamp` للحفاظ على أقدمية البيعة.
- **التسجيل الذكي (Smart Logging):** يتم بناء السجل ديناميكياً ليقوم بمقارنة القيم (قبل وبعد) وعرض **فقط القيم التي تم تغييرها فعلياً** (لتجنب التشتت البصري)، ويحفظ السجل بهيئة HTML في `editLog`.
- **نافذة السجل (Edit Log Modal):** يُعرض السجل عبر نافذة منبثقة (Modal) تفتح عند الضغط على شارة "مُعدّل". تحتوي النافذة على زر حذف لكل سجل، ويعمل الحذف بنظام (Optimistic UI) لسرعة استجابة الواجهة فوراً بينما يتم الحفظ في الخلفية.
- **ثبات وقت البيعة عند التأكيد:** عند تأكيد البيعة المؤقتة (سواء بحفظها كفاتورة `inv_saveInvoice`، أو تحويلها لدين `convertPendingSaleToDebt`)، **يُمنع منعاً باتاً** توليد وقت جديد لـ `timestamp`. يجب توريث الوقت الأصلي من `pendingSaleOriginData.timestamp` لكي تظهر في تقرير المبيعات بنفس دقيقة إنشائها، بينما يتم استخدام `new Date()` للخاصية `confirmedAt` فقط لغرض التراجع.

## 🔄 3. نظام التراجع والتسجيل (Undo/Redo & Snapshots)
- النظام يعتمد على `stateHistory` وأخذ لقطات (Snapshots) لكل المتغيرات قبل أي حركة.
- عند إضافة أي مصفوفة أو متغير شامل جديد للنظام (مثل `pendingReturns` أو غيرها)، يجب أن تتذكر إضافته في الدالتين: `createStateSnapshot()` و `loadStateFromSnapshot()` لضمان عمل أزرار التراجع بكفاءة تامة.
- **أي وظيفة تقوم بتعديل البيانات يجب أن تستدعي `saveStateToHistory()` في بدايتها لحفظ الحالة، وتستدعي `updateUndoRedoButtons()` في نهايتها.**

---
**رسالة إلى الذكاء الاصطناعي:** راجع هذا الملف دائماً واقرأه بتمعن قبل المساس بأي من وظائف `finance-core.js` لضمان عدم كسر أي من هذه الميزات المحمية.
